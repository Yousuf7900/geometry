function rectangleAreaCalculation(){
    // width
    const rectangleWidthValue = document.getElementById('rectangle-width').ariaValueMax;
    const rectangleWidth = parseFloat(rectangleWidthValue);

    // length
    const rectangleLengthValue = document.getElementById('rectangle-length').ariaValueMax;
    const rectangleLength = parseFloat(rectangleLengthValue);

    // Area
    const Area = rectangleWidth * rectangleLength;

    // Display
    
}