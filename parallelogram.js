function parallelogramAreaCalculate(){
    // base
    const parallelogramBaseVAlue = document.getElementById('parallelogram-base').value;
    const parallelogramBase = parseFloat(parallelogramBaseVAlue);

    // height
    const parallelogramHeightValue = document.getElementById('parallelogram-height').value;
    const parallelogramHeight = parseFloat(parallelogramHeightValue);

    // Area
    const Area = parallelogramBase * parallelogramHeight;

    // Display
    
}